# Compiled binaries
/card-reader
/orga-probe

# Screenshot — contains personal insurance data, do not commit
/gkv-card-reader.png

# macOS
.DS_Store
.history

# KTDA — regenerated via `card-reader ktda update`
/ktda-files/

# Card-reader output files — keep the directory, ignore its contents
/output/*
!/output/.gitkeep
pkg/reader/usb/testdata/ioreg_with_orga.txt
